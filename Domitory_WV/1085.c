/*
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {

	int x, y, w, h;

	scanf("%d %d %d %d", &x, &y, &w, &h);

	int slest = x;

	if ((w - x)<slest) {
		slest = w - x;
	}
	if ((h - y) < slest) {
		slest = h - y;
	}
	if (y < slest) {
		slest = y;
	}

	printf("%d", slest);

}*/